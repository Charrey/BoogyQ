int a.
bool c.
if(True):
	if(a <= 10):
	<  	loop.
	a + 1 -> a.
	if(!bool c):
	    if(a <= 20):
    <-	    eloop.
	True -> c.

int b.
bool d.
if(True):
	if(b <= 10):
		break.
	b + 1 -> b.
	if(!bool d):
	    if(b <= 20):
                        <-	    break.
	True -> d.
	
