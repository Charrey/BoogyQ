package checking;

/** Pascal data type kind. */
public enum TypeKind {
	/** Integer base type. */
	INT,
	/** Boolean base type. */
	BOOL,
	/** Char base type. */
	CHAR,
	/** String base type. */
	STRING,
	/** Array compound type. */
	ARRAY;
}
