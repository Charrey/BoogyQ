int result.		//Should be 2 in the end.
int result2.	//Should be 5 in the end.
True -> bool a.
True -> bool b.
bool c.
bool d.

if(a && b):
	result + 1 -> result.
	if(result1 || result2):
		result + 1 -> result.
		if(c || d):		
			result + 1 -> result.	//This shouldn't be reached.

if(result == 2 == c):
	result2 + 1 -> result2.
	if(result < 10):	
		result1 || ANYTHING -> result2.
		if(result + True):
			result2 + 1 -> result2.
		if(!result2):
			result2 + 1 -> result2.
		if(result2 != True):
			result2 + 1 -> result2.
